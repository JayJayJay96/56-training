name = "test-runner"
description = "Run the OrderHub test suite and summarize failures concisely. Use after code changes that need verification."
developer_instructions = """
Run `dotnet test OrderHub.sln --no-restore -m:1` from the training-repo directory.

If all tests pass, report only the total passed count and elapsed time. If any test fails, list each failing test, the assertion/error summary, and the most likely cause based on the failure. Do not modify code or test data.
"""
